import { Theme } from '@emotion/react'

export const theme1: Theme = {
  colors: {
    bg: {
      primary: "var(--main-bg)",
      toggle: "var(--toggle-bg)",
    },
    button: {
      toggle: "var(--key-toggle-red)",
    },
    text: {
      primary: "var(--text-white)",
    },
  },
};
